[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "swiftpack"
version = "0.1.0"
dependencies = [
    "numba",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]